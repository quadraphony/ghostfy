# Bridge Registry

Use `ghostify bridges` to list the registered bridge runners and their statuses:

```bash
ghostify bridges
```

Outputs metadata for:

- `openvpn-bridge`
- `ssh-bridge`
- `udp2raw-bridge`
